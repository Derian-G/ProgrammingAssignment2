MAKECACHEMATRIX
Make a list with: Set and get the value of the vector, set and get the value of the mean
> makeCacheMatrix <- function(x = matrix()) {
+     inv_x<- NULL
+     set <- function(j) {
+         x <<- j
+         inv_x <<- NULL
+     }
+     get <- function() x
+     setinv <- function(inverse) inv_x <<- inverse
+     getinv <- function() inv_x
+     list(set = set,
+          get = get,
+          setinv = setinv,
+          getinv = getinv)
+ }
This function computes the inverse of the special "matrix" returned by makeCacheMatrix above.
> cacheSolve <- function(x, ...) {
+     inv_x <- x$getinv()
+     if (!is.null(inv_x)) {
+         message("getting cached data")
+         return(inv_x)
+     }
+     m <- x$get()
+     inv_x <- solve(m, ...)
+     x$setinv(inv_x)
+     inv_x
+ }
